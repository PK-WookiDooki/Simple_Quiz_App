import { Quiz } from "../features";

const Home = () => {
    return (
        <section className=" w-[90%] md:w-[80%] mx-auto py-5">
            <Quiz />
        </section>
    );
};

export default Home;
